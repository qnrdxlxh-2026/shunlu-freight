.detail-page { padding: 24rpx; background: #f5f5f5; min-height: 100vh; }
.status-bar { padding: 32rpx; border-radius: 16rpx; margin-bottom: 24rpx; text-align: center; }
.status-bar.badge-orange { background: linear-gradient(135deg,#fa8c16,#d46b08) }
.status-bar.badge-blue { background: linear-gradient(135deg,#1890ff,#096dd9) }
.status-bar.badge-green { background: linear-gradient(135deg,#52c41a,#389e0d) }
.status-text { color: #fff; font-size: 36rpx; font-weight: 600 }

/* 取货码/送达码卡片 */
.code-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 32rpx 24rpx;
  margin-bottom: 24rpx;
  text-align: center;
  border: 2rpx solid;
}
.code-card.pickup-card { border-color: #fa8c16; background: linear-gradient(135deg,#fff8f0,#fff); }
.code-card.delivery-card { border-color: #52c41a; background: linear-gradient(135deg,#f6fff0,#fff); }
.code-label { font-size: 26rpx; color: #999; margin-bottom: 12rpx; }
.code-number { font-size: 64rpx; font-weight: 700; color: #333; letter-spacing: 8rpx; margin-bottom: 8rpx; }
.pickup-card .code-number { color: #fa8c16; }
.delivery-card .code-number { color: #52c41a; }
.code-hint { font-size: 24rpx; color: #aaa; }

.route-section { background: #fff; border-radius: 16rpx; padding: 32rpx; margin-bottom: 24rpx; }
.route-item { display: flex; align-items: flex-start }
.dot { width: 24rpx; height: 24rpx; border-radius: 50%; margin-right: 16rpx; margin-top: 6rpx }
.dot.green { background: #52c41a }
.dot.red { background: #f5222d }
.addr { flex: 1; font-size: 28rpx; color: #333 }
.route-line { width: 4rpx; height: 40rpx; background: #e8e8e8; margin-left: 10rpx }

.info-section { background: #fff; border-radius: 16rpx; padding: 24rpx; margin-bottom: 24rpx; }
.info-item { display: flex; justify-content: space-between; padding: 16rpx 0; border-bottom: 1rpx solid #f0f0f0 }
.info-item:last-child { border-bottom: none }
.label { font-size: 28rpx; color: #666 }
.value { font-size: 28rpx; color: #333 }
.value.price { color: #52c41a; font-weight: 600 }

/* 联系方式卡片 */
.contact-section { margin-bottom: 24rpx }
.contact-card { background: #fff; border-radius: 16rpx; padding: 24rpx }
.contact-label { font-size: 28rpx; color: #666; margin-bottom: 12rpx; }
.contact-info { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8rpx; }
.contact-phone { font-size: 32rpx; font-weight: 600; color: #333 }
.contact-btns { display: flex; gap: 16rpx; }
.btn-virtual, .btn-direct {
  font-size: 26rpx; border-radius: 8rpx; padding: 12rpx 20rpx; text-align: center;
}
.btn-virtual { background: #e6f7ff; color: #1890ff; }

/* 中途装卸点样式 */
.waypoint-item {
  margin-top: 8rpx;
}
.waypoint-dot {
  background: #1890ff;
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  border: 3rpx solid #fff;
  box-shadow: 0 0 0 2rpx #1890ff;
}
.waypoint-line {
  background: #1890ff;
  opacity: 0.5;
}
.waypoint-addr {
  flex: 1;
  padding: 8rpx 0;
}
.waypoint-label {
  display: inline-block;
  font-size: 24rpx;
  background: #1890ff;
  color: #fff;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  margin-bottom: 8rpx;
}
.waypoint-address {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 4rpx;
}
.waypoint-contact {
  font-size: 24rpx;
  color: #666;
}
.waypoint-verify-box {
  display: flex;
  align-items: center;
  margin-top: 16rpx;
  gap: 12rpx;
}
.waypoint-verify-input {
  flex: 1;
  font-size: 28rpx;
  padding: 12rpx 16rpx;
  background: #f5f5f5;
  border-radius: 8rpx;
}
.waypoint-verify-btn {
  font-size: 28rpx;
  background: #1890ff;
  color: #fff;
  padding: 12rpx 24rpx;
  border-radius: 8rpx;
}
.btn-direct { background: #f5f5f5; color: #666; }
.contact-hint { font-size: 22rpx; color: #bbb; margin-top: 4rpx; }

.action-section { margin-top: 32rpx }
.scan-btn, .pickup-btn, .deliver-btn { border-radius: 50rpx; padding: 28rpx 0; font-size: 32rpx; text-align: center; border: none; margin-top: 16rpx }
.scan-btn { background: #1890ff; color: #fff }
.pickup-btn { background: #fa8c16; color: #fff }
.deliver-btn { background: #52c41a; color: #fff }
/* Phase 4: GPS按钮样式 */
.gps-btn, .track-btn {
  border-radius: 50rpx;
  padding: 20rpx 0;
  font-size: 30rpx;
  text-align: center;
  border: none;
  margin-bottom: 16rpx;
}
.gps-btn { background: #1890ff; color: #fff; }
.track-btn { background: #722ed1; color: #fff; }

/* 导航按钮 */
.nav-btn {
  margin-top: 16rpx;
  background: #13c2c2;
  color: #fff;
  font-size: 28rpx;
  padding: 16rpx 0;
  border-radius: 8rpx;
  text-align: center;
}

/* 取件码输入区域 */
.pickup-code-input-section {
  margin-top: 24rpx;
  padding: 24rpx;
  background: #f6ffed;
  border-radius: 8rpx;
  border: 1rpx solid #b7eb8f;
}
.pickup-code-label {
  font-size: 26rpx;
  color: #52c41a;
  margin-bottom: 12rpx;
}
.pickup-code-input {
  font-size: 36rpx;
  padding: 16rpx 24rpx;
  background: #fff;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  text-align: center;
  letter-spacing: 8rpx;
  margin-bottom: 16rpx;
}
.confirm-delivery-btn {
  background: #52c41a;
  color: #fff;
  font-size: 32rpx;
  padding: 20rpx 0;
  border-radius: 8rpx;
  text-align: center;
}
